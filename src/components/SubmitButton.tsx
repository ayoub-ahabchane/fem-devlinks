import React from 'react'

const SubmitButton = () => {
  return (
    <button
      type="submit"
      className={`button primary w-full md:w-auto`}
      
    >
      Save
    </button>
  )
}

export default SubmitButton